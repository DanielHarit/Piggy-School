import { makeStyles } from '@mui/styles'

export default makeStyles(() => ({
  root: {},
  storyBtn: {
    padding: 0,
    minWidth: 40,
    width: 40,
    height: 40,
    borderRadius: '50%',
    backgroundColor: 'salmon',
  },
}))
